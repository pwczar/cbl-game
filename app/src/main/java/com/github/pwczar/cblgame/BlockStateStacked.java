package com.github.pwczar.cblgame;

/**
 * The state of a Block that is stacked on the grid.
 */
public class BlockStateStacked extends BlockState {
    BlockStateStacked(Block block) {
        super(block);
    }
}
